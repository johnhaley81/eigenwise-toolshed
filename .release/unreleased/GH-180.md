---
ref: GH-180
title: integrate no longer refuses a candidate that changed its implicitly admitted release fragment
bump: patch
plugins: [sidequest]
---

Wave assembly derived its declared surfaces from a fresh read of ticket.files, so the release fragment the commit gate and the stored-range validator both admit implicitly read as surface_overlap at integrate time. The wave surface now uses the same ticketCommitScope derivation plus the candidate's recorded admitted scope, and a wave_invalidated refusal names each candidate's reason and offending paths instead of printing baselines that match.
